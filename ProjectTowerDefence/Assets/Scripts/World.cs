using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class World : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Grid worldGrid = new Grid(2, 5, 10f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
